# PicoClaw Go extensibility

Compile-time cm2labs functionality lives in this package tree. New Go files are included in a binary when their package is reachable from the selected command through imports.

Keep manifests and architecture documentation under `cm2labs/extensibility/`; keep executable Go code here.
