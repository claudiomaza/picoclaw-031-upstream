# PicoClaw cm2labs overlay

This directory owns cm2labs contracts, manifests and documentation. The current compile-time integration remains in `pkg/tools` and `pkg/agent` until a tested migration moves it.
